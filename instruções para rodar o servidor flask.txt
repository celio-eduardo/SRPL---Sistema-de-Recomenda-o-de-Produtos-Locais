Primeiro instalar tudo que está contido no arquivo requirements.txt na máquina:

pip install -r requirements.txt

Executar a aplicação:

python -m src.main

Acesse o navegador no link:

http://localhost:5000